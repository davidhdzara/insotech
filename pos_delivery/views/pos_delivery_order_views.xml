<odoo>
  <data>
    
    <!-- Kanban View - Main view with states -->
    <record id="view_pos_delivery_order_kanban" model="ir.ui.view">
      <field name="name">pos.delivery.order.kanban</field>
      <field name="model">pos.delivery.order</field>
      <field name="arch" type="xml">
        <kanban default_group_by="state" class="o_kanban_small_column" quick_create="false">
          <field name="name"/>
          <field name="partner_id"/>
          <field name="delivery_person_id"/>
          <field name="state"/>
          <field name="priority"/>
          <field name="color"/>
          <field name="time_elapsed"/>
          <field name="order_total"/>
          <field name="delivery_cost"/>
          <field name="rating"/>
          <field name="delivery_zone_id"/>
          
          <progressbar field="priority" colors='{"3": "danger", "2": "warning", "1": "success", "0": "muted"}'/>
          
          <templates>
            <t t-name="card">
              <div t-attf-class="oe_kanban_card oe_kanban_global_click #{record.color.raw_value > 0 ? 'oe_kanban_color_' + record.color.raw_value : ''}">
                <div class="oe_kanban_content">
                  
                  <!-- Header with priority badge -->
                  <div class="o_kanban_record_top mb-2">
                    <div class="o_kanban_record_headings">
                      <strong class="o_kanban_record_title">
                        <field name="name"/>
                      </strong>
                      <div class="text-muted">
                        <i class="fa fa-clock-o"/> <field name="time_elapsed"/>
                      </div>
                    </div>
                    <div class="o_kanban_record_top_right">
                      <field name="priority" widget="priority"/>
                    </div>
                  </div>

                  <!-- Customer Info -->
                  <div class="mb-2">
                    <i class="fa fa-user"/> <field name="partner_id"/>
                  </div>

                  <!-- Delivery Person -->
                  <div t-if="record.delivery_person_id.raw_value" class="mb-2">
                    <i class="fa fa-motorcycle"/> <field name="delivery_person_id"/>
                  </div>

                  <!-- Zone -->
                  <div t-if="record.delivery_zone_id.raw_value" class="mb-2">
                    <i class="fa fa-map-marker"/> <field name="delivery_zone_id"/>
                  </div>

                  <!-- Order Amount -->
                  <div class="mb-2">
                    <strong>
                      <i class="fa fa-money"/>
                      <field name="order_total" widget="monetary"/>
                      <t t-if="record.delivery_cost.raw_value > 0">
                        + <field name="delivery_cost" widget="monetary"/> (delivery)
                      </t>
                    </strong>
                  </div>

                  <!-- Rating -->
                  <div t-if="record.rating.raw_value" class="mb-2">
                    <field name="rating" widget="badge"/>
                  </div>

                  <!-- Footer with actions -->
                  <div class="o_kanban_record_bottom mt-2">
                    <div class="oe_kanban_bottom_left">
                      <button t-if="record.state.raw_value == 'pending'" 
                              name="action_assign" 
                              type="object" 
                              class="btn btn-sm btn-primary">
                        Assign
                      </button>
                      <button t-if="record.state.raw_value == 'assigned'" 
                              name="action_start_transit" 
                              type="object" 
                              class="btn btn-sm btn-success">
                        Start Delivery
                      </button>
                      <button t-if="record.state.raw_value == 'in_transit'" 
                              name="action_complete" 
                              type="object" 
                              class="btn btn-sm btn-success">
                        Complete
                      </button>
                    </div>
                    <div class="oe_kanban_bottom_right">
                      <field name="activity_ids" widget="kanban_activity"/>
                    </div>
                  </div>

                </div>
              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>

    <!-- List View -->
    <record id="view_pos_delivery_order_list" model="ir.ui.view">
      <field name="name">pos.delivery.order.list</field>
      <field name="model">pos.delivery.order</field>
      <field name="arch" type="xml">
        <list string="Delivery Orders" decoration-info="state=='pending'" 
              decoration-warning="state=='assigned'" decoration-success="state=='completed'"
              decoration-danger="state=='failed'">
          <field name="name" string="Number"/>
          <field name="create_date" string="Created"/>
          <field name="partner_id" string="Customer"/>
          <field name="delivery_person_id" string="Delivery Person"/>
          <field name="delivery_zone_id" string="Zone"/>
          <field name="priority" widget="priority"/>
          <field name="state" widget="badge" 
                 decoration-info="state=='pending'"
                 decoration-warning="state=='assigned'"
                 decoration-primary="state=='in_transit'"
                 decoration-success="state=='completed'"
                 decoration-danger="state=='failed'"/>
          <field name="time_elapsed" string="Time"/>
          <field name="order_total" string="Amount" widget="monetary"/>
          <field name="rating" optional="hide"/>
        </list>
      </field>
    </record>

    <!-- Form View - Detailed view -->
    <record id="view_pos_delivery_order_form" model="ir.ui.view">
      <field name="name">pos.delivery.order.form</field>
      <field name="model">pos.delivery.order</field>
      <field name="arch" type="xml">
        <form string="Delivery Order">
          <header>
            <!-- Status Bar -->
            <field name="state" widget="statusbar" statusbar_visible="pending,assigned,in_transit,completed"/>
            
            <!-- Action Buttons -->
            <button name="action_assign" string="Assign Delivery" type="object" 
                    class="btn-primary" invisible="state != 'pending'"/>
            <button name="action_start_transit" string="Start Delivery" type="object" 
                    class="btn-success" invisible="state != 'assigned'"/>
            <button name="action_complete" string="Mark as Completed" type="object" 
                    class="btn-success" invisible="state != 'in_transit'"/>
            <button name="action_fail" string="Mark as Failed" type="object" 
                    class="btn-danger" invisible="state not in ['assigned', 'in_transit']"/>
            <button name="action_reset_to_pending" string="Reset to Pending" type="object" 
                    invisible="state == 'pending'"/>
          </header>

          <sheet>
            <!-- Smart Buttons -->
            <div class="oe_button_box" name="button_box">
              <button name="action_open_pos_order" type="object" 
                      class="oe_stat_button" icon="fa-shopping-cart">
                <div class="o_field_widget o_stat_info">
                  <span class="o_stat_text">POS Order</span>
                </div>
              </button>
            </div>

            <!-- Title -->
            <div class="oe_title">
              <h1>
                <field name="name" readonly="1"/>
              </h1>
            </div>

            <!-- Priority and Time Info -->
            <group>
              <group>
                <field name="priority" widget="priority"/>
                <field name="time_elapsed" widget="char"/>
              </group>
              <group>
                <field name="create_date" readonly="1"/>
                <field name="completed_date" readonly="1" invisible="state not in ['completed', 'failed']"/>
              </group>
            </group>

            <notebook>
              <!-- Delivery Information -->
              <page string="Delivery Info" name="delivery_info">
                <group>
                  <group string="Customer Information">
                    <field name="partner_id" options="{'no_create': True}"/>
                    <field name="delivery_address" placeholder="Full delivery address..."/>
                    <field name="delivery_phone" widget="phone"/>
                    <field name="customer_notes" placeholder="Special instructions..."/>
                  </group>

                  <group string="Delivery Details">
                    <field name="delivery_person_id" 
                           options="{'no_create_edit': True}"
                           domain="[('is_delivery_person', '=', True)]"/>
                    <field name="delivery_zone_id"/>
                    <field name="delivery_cost" widget="monetary"/>
                    <field name="estimated_delivery_time"/>
                  </group>
                </group>

                <group>
                  <group string="Location" name="location">
                    <field name="delivery_latitude"/>
                    <field name="delivery_longitude"/>
                  </group>
                </group>
              </page>

              <!-- Order Details -->
              <page string="Order Details" name="order_details">
                <group>
                  <group>
                    <field name="pos_order_id" readonly="1"/>
                    <field name="order_total" widget="monetary"/>
                    <field name="currency_id" invisible="1"/>
                  </group>
                  <group>
                    <field name="assigned_date" readonly="1"/>
                    <field name="in_transit_date" readonly="1"/>
                    <field name="total_delivery_time" widget="float_time" 
                           invisible="state != 'completed'"/>
                  </group>
                </group>
              </page>

              <!-- Comments -->
              <page string="Comments" name="comments">
                <group>
                  <field name="warehouse_notes" placeholder="Notes from warehouse/kitchen staff..." 
                         groups="point_of_sale.group_pos_manager,point_of_sale.group_pos_user"/>
                  <field name="delivery_notes" placeholder="Notes from delivery person..."/>
                </group>
              </page>

              <!-- Proof of Delivery -->
              <page string="Proof of Delivery" name="proof" 
                    invisible="state not in ['completed', 'failed']">
                <group>
                  <group string="Photo Evidence">
                    <field name="delivery_photo" widget="image" class="oe_avatar"/>
                    <field name="delivery_photo_filename" invisible="1"/>
                  </group>
                  <group string="Signature">
                    <field name="signature" widget="image"/>
                  </group>
                </group>
              </page>

              <!-- Rating -->
              <page string="Rating" name="rating" invisible="state != 'completed'">
                <group>
                  <field name="rating" widget="radio"/>
                  <field name="rating_comment" placeholder="Customer feedback..."/>
                </group>
              </page>

            </notebook>
          </sheet>

          <!-- Chatter -->
          <chatter/>
        </form>
      </field>
    </record>

    <!-- Search View -->
    <record id="view_pos_delivery_order_search" model="ir.ui.view">
      <field name="name">pos.delivery.order.search</field>
      <field name="model">pos.delivery.order</field>
      <field name="arch" type="xml">
        <search string="Search Delivery Orders">
          <field name="name" string="Delivery Number"/>
          <field name="partner_id" string="Customer"/>
          <field name="delivery_person_id" string="Delivery Person"/>
          <field name="pos_order_id" string="POS Order"/>
          <field name="delivery_zone_id" string="Zone"/>
          
          <filter string="Pending" name="pending" domain="[('state', '=', 'pending')]"/>
          <filter string="Assigned" name="assigned" domain="[('state', '=', 'assigned')]"/>
          <filter string="In Transit" name="in_transit" domain="[('state', '=', 'in_transit')]"/>
          <filter string="Completed" name="completed" domain="[('state', '=', 'completed')]"/>
          <filter string="Failed" name="failed" domain="[('state', '=', 'failed')]"/>
          
          <separator/>
          <filter string="Urgent" name="urgent" domain="[('priority', '=', '3')]"/>
          <filter string="High Priority" name="high" domain="[('priority', '=', '2')]"/>
          
          <separator/>
          <filter string="Today" name="today" domain="[('create_date', '&gt;=', datetime.datetime.now().replace(hour=0, minute=0, second=0))]"/>
          <filter string="This Week" name="week" domain="[('create_date', '&gt;=', (context_today() - datetime.timedelta(days=7)).strftime('%Y-%m-%d'))]"/>
          
          <group expand="0" string="Group By">
            <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
            <filter string="Delivery Person" name="group_delivery_person" context="{'group_by': 'delivery_person_id'}"/>
            <filter string="Zone" name="group_zone" context="{'group_by': 'delivery_zone_id'}"/>
            <filter string="Priority" name="group_priority" context="{'group_by': 'priority'}"/>
            <filter string="Created Date" name="group_create_date" context="{'group_by': 'create_date'}"/>
          </group>
        </search>
      </field>
    </record>

    <!-- Action -->
    <record id="action_pos_delivery_order" model="ir.actions.act_window">
      <field name="name">Delivery Orders</field>
      <field name="res_model">pos.delivery.order</field>
      <field name="view_mode">kanban,list,form</field>
      <field name="search_view_id" ref="view_pos_delivery_order_search"/>
      <field name="context">{'search_default_pending': 1, 'search_default_assigned': 1, 'search_default_in_transit': 1}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          Create your first delivery order
        </p>
        <p>
          Delivery orders are created from POS orders. Click the "Create Delivery" button on any POS order.
        </p>
      </field>
    </record>

  </data>
</odoo>

