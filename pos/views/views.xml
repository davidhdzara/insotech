<odoo>
  <data>
    <!-- List view -->
    <record model="ir.ui.view" id="test_record_list">
      <field name="name">test.record.list</field>
      <field name="model">test.record</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="description"/>
        </list>
      </field>
    </record>

    <!-- Form view -->
    <record model="ir.ui.view" id="test_record_form">
      <field name="name">test.record.form</field>
      <field name="model">test.record</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group>
              <field name="name"/>
              <field name="description"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Action -->
    <record model="ir.actions.act_window" id="action_test_record">
      <field name="name">Test Records</field>
      <field name="res_model">test.record</field>
      <field name="view_mode">list,form</field>
    </record>

    <!-- Menu -->
    <menuitem name="Test" id="menu_test_root"/>
    <menuitem name="Test Records" id="menu_test_records" parent="menu_test_root" action="action_test_record"/>
  </data>
</odoo>